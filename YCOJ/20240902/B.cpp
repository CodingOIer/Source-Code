#include <cstdio>
#include <cstring>
constexpr int MaxN = 5e5 + 5;
int n, q;
char s[MaxN];
int main()
{
    scanf("%s", s + 1);
    n = strlen(s + 1);
    for (int i = 1; i <= q; i++)
    {
        int l, r;
        scanf("%d%d", &l, &r);
    }
    return 0;
}