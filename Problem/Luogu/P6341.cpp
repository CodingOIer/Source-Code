#include <cmath>
#include <cstdio>
#include <map>
constexpr int MaxN = 1e3 + 5e2 + 5;
int n;
int x[MaxN];
int y[MaxN];
int main()
{
    scanf("%d", &n);
    for (int i = 1; i <= n; i++)
    {
        scanf("%d%d", &x[i], &y[i]);
    }
    for (int i = 1; i <= n; i++)
    {
        for (int j = i + 1; j <= n; j++)
        {
        }
    }
    return 0;
}