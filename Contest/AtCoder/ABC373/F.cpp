#include <cstdio>
constexpr int MaxN = 3e3 + 5;
constexpr int Choose = 1e3;
int n, m;
int w[MaxN];
int v[MaxN];
int dp[MaxN][MaxN];
int main()
{
    scanf("%d%d", &n, &m);
    for (int i = 1; i <= n; i++)
    {
        scanf("%d%d", &w[i], &v[i]);
    }
    for (int i = 1; i <= n; i++)
    {
        for (int j = 0; j <= Choose; j++)
        {
            for (int k =)
        }
    }
    return 0;
}